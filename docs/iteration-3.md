# Звіт по ітерації 3 (Lab 36)

## 1. Покриття коду (Coverage)
Завдяки написанню нових тестів, поточні показники покриття коду становлять:
* Line Coverage (Рядки): 86.53%
* Branch Coverage (Гілки): 64.06%
* Method Coverage (Методи): 77.77%

## 2. Кількість тестів
Всього у проєкті написано та успішно проходить 53 тести :
* Unit Tests: 42 тести (включаючи перевірку прикордонних значень, використання `[Theory]`, перевірку доменних винятків).
* Integration Tests: 11 тестів (повний цикл роботи з файловою системою, імітація I/O помилок, перевірка fallback-механізмів).

## 3. Які Smells  були усунені
Під час цієї ітерації було покращено архітектуру:
* Приховані залежності: Відокремлено логіку від UI (усі `Console.ReadLine` залишилися виключно в шарі інтерфейсу, сервіси працюють тільки з чистими даними).
* Magic Numbers / Strings: Відбувся повний перехід на використання Enum (`TaskStatus`, `TaskPriority`) замість "сирих" текстових статусів.
* Повернення кодів помилок: Замінено слабку практику використання `out string error` на чітку ієрархію доменних винятків (наприклад, `DuplicateTaskTitleException`).

## 4. Які покращення треба завершити на Lab 37
Хоча критична логіка покрита тестами, а інфраструктура захищена від збоїв файлової системи, залишаються зони для покращення в фінальній ітерації:
1. Патерн "Репозиторій": Наразі репозиторій має жорстку прив'язку до файлів. Потрібно виділити інтерфейс `ITaskRepository` для повної інверсії залежностей (Dependency Inversion).
2. Асинхронність UI: Консольне меню викликає асинхронні методи репозиторію. Треба перевести метод `Main` у `Program.cs` на `async Task Main`, щоб уникнути синхронних блокувань.
3. Глобальне логування: Замість простого виводу помилок I/O в консоль, необхідно реалізувати повноцінний логер (наприклад, Serilog або NLog) для запису критичних збоїв у окремий лог-файл.